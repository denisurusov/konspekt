## konspekt project state

This repo carries konspekt-format project state under `.konspekt/`. The agent
operating instructions live in `AGENTS.md` at the repo root — read that file and
follow it. It is the single source, so this tool-specific file never drifts from
it.

@AGENTS.md
