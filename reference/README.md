# reference

Placeholder for the konspekt reference implementation.

For an open standard, the reference implementation is half the game: a spec without a working tool that delivers real value on day one mostly doesn't get adopted, because nobody implements against a promise. This directory is where that tool will live.

Not started yet. The current priority is refining the format by dogfooding (see `../instance/`).
