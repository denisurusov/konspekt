<!-- konspekt source excerpt
     Verbatim exchange on correcting the notifier event set, konspekt,
     2026-07-19. Both sides included. This header is the only editorial text;
     everything below the rule is verbatim as written by each party, with
     omissions within a turn marked […]. Nothing is paraphrased. -->

---

**Claude:** "[…] `notify.yml` defines **created** as *an id absent from the parent commit, with `review: accepted`*. That was correct when I wrote it — entities were born accepted. They aren't any more. Under the new discipline an atom is born `proposed`, and acceptance arrives later as an edit to an existing file. So: born-proposed atom → id is new, but review is `proposed` → doesn't fire; later accepted → id isn't new → doesn't fire. Nothing fires on the main path. The config went stale within hours of being written, which is precisely the risk `nw-notify-config-precedes-consumer` flagged at confidence 0.55 — and it's now been demonstrated rather than speculated. […] The notifiable moment isn't creation, it's **an entity becoming accepted** — whether at birth or by a later edit. Keyed on `id`, as `nw-rename-fires-as-creation` established. Worth noting why this doesn't contradict the earlier reasoning. We rejected watching status transitions because `status` is written at birth and never moves. That's still true of `status`. `review` is the one field that genuinely transitions […]."

**User:** update notify.yml for to email when noteworthies are accepted. then create the actual hook

**User:** let's test it. Add and write down a test noteworthy - I'll expect an email
