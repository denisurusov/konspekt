# Source excerpt — delete reference/, triggers collapse (2026-06-22)

Conversation: konspekt / "setup and operating loop". Verbatim spans, both sides,
the seam-closure decision was drawn from. This file's git blob SHA is the
`contentHash` the decision entities carry.

## Human — delete reference; when-to-fire is human-vs-machine; verbs alias the API
it is really tempting to just delete reference. to answer your question directly
the "when to fire" is a guideline and it is a simple one - decision by human vs
decision by machine. *how* this is done is implementation-specific. We added
verbs (sync) to the spec mostly as an alias for future API calls

## Assistant — "triggers" was a phantom category; collapse into propose-accept
"'when to fire' reduces to human-decision vs machine-decision. But notice what
that is: it's the propose-accept separation, which is already a normative spec
principle ... The machine ventures a proposal on its own judgment; the human's
command is the durable write. That's propose-accept viewed from the 'what
triggers a write' angle — not a separate concept. Triggers was a phantom
category."

"sync/persist are spec-level aliases for the future API — the contract surface.
The spec owns the verb names (and the propose-accept meaning behind them); the
mechanics behind each verb are implementation."

"the only legitimate tenant left for reference/ is not-yet-written code behind
the verb-API — and an empty folder holding a maybe-someday tool is precisely the
speculative scaffolding that just cost us a whole task to untangle ... delete it.
The honest reference a second implementer conforms to today is spec/ + the
conformant dogfood instance + the manual-reupload probe."

## Human — delete
delete
