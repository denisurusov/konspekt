# Source excerpt — venturing must be a forcing skill (2026-06-23)

Conversation: konspekt / "setup and operating loop". Verbatim spans, both sides,
the decision was drawn from. This file's git blob SHA is the `contentHash` the
entity carries.

## Human — add a forcing skill for the venture, like what you did
But - to your point - we need to add very solid instruction/skill forcing LLMs
to analyze each iteration and , based on the results, prompt the user with
"looks like a great time to sync these atoms to these nodes" - just like what you
did

## Assistant — relying on spontaneous venturing is the unreliable mode
"relying on the maintainer to spontaneously venture captures is the unreliable
mode (this very session, the setup kit slipped through uncaptured until you
flagged it). The fix is to make the atom-readiness trigger a forcing skill, not
emergent behavior."
