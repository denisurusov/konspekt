<!-- konspekt source excerpt
     Verbatim exchange on writing notify.yml before any watcher exists,
     konspekt, 2026-07-19. Both sides included. This header is the only
     editorial text; everything below the rule is verbatim as written by each
     party, with omissions within a turn marked […]. Nothing is paraphrased. -->

---

**Claude:** "[…] **`nw-notify-config-precedes-consumer`** (`kind: decision`, confidence 0.55). The claim: writing the config file before any watcher exists is the right order, because the file is the contract and the reasoning is freshest now. Confidence is low because the opposite case is real — an unread config can drift from whatever eventually reads it, and konspekt already has an atom warning about exactly that failure mode in `nw-derive-not-copy`. […] I'll write it `review: proposed` with that confidence when you say go — or say skip and it doesn't get written. Either is a legitimate disposition; that's the point of the queue."

**User:** Write it down
