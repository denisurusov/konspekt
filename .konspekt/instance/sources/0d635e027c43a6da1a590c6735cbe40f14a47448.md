# Source excerpt — distribution is derived, not copied (2026-06-23)

Conversation: konspekt / "setup and operating loop". Verbatim spans, both sides,
the decision was drawn from. This file's git blob SHA is the `contentHash` the
entity carries.

## Human — a /distribution directory holding the outward-facing copy
how about we create a /distribution directory and copy the "final" version of
the specs, agent skills, scripts there? basically the working copies live under
konspekt root, and whatever gets scaffolded or referenced from outside goes into
/distribution. we can then worry about versioning distributions

## Assistant — "copy" creates a second spec; derive instead
"'Copy the specs there' creates a second spec. The moment /distribution/spec/ is
a hand-maintained copy of /spec/, you have two sources of truth that drift —
which is precisely the dual-authority problem that reference/ became ... The fix
is the konspekt move we already use elsewhere: derive, don't copy. ... a
build/distribute.mjs that assembles the outward-facing subset ... into
/distribution ... Root stays the single source of truth; /distribution is a
regenerable projection."

## Human — build it
build the machinery now
