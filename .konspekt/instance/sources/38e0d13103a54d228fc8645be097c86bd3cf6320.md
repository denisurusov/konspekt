<!-- konspekt source excerpt
     Verbatim transcript of the full-instance conformance enumeration,
     konspekt, 2026-07-19. Both sides included. This header is the only
     editorial text; everything below the rule is verbatim as written by each
     party, with omissions within a turn marked […]. No included line is
     paraphrased. -->

---

**User:** just finish the job

**Claude:** "konspekt turned out to be publicly readable via raw, so I pulled all 82 instance files and parsed the front-matter mechanically rather than by sample. code_tracer is private; that half is MCP reads. […] The status-by-kind row is the striking one: konspekt carries `status` exactly where `schema.ts` defines a vocabulary and nowhere else. That isn't approximate conformance, it's exact. **The verdict on R1–R8: seven of eight are dead.** R1 (envelope) — konspekt has no envelope outside nodes. It was code_tracer's local choice, never convergent practice. R2 (status optional) — already true and already implemented; not a change. R3 (kind via directory) — unnecessary; `kind` is present on 100% of konspekt's noteworthy and waypoints. R4 (edge generalization) — would break 25+ conforming rows to match one instance. R5 (artifact content-bearing) — konspekt's twelve artifacts are 313–1048 byte pointers with `name`/`kind`/`location`; code_tracer's are 2.3–27KB inline documents. R6 (drop `Waypoint.timestamp`) — all eleven have it. R7 (concept) — eleven concepts, all wired via `mentions`; the entity type is healthy, code_tracer simply hasn't used it. R8, the validator, survives — and is now the only item, considerably strengthened. Every divergence I attributed to 'the spec being behind' is code_tracer drifting unchecked. **Two findings that are real and weren't in the recon.** Filename ≠ id in 48 of 82 files. `SERIALIZATION.md` requires equality; artifacts, concepts, and noteworthy all strip their type prefix from the filename while keeping it in the id. Nodes and waypoints comply. Either the rule changes or 48 files get renamed — and I added five more to the wrong side of it today. Only 26 of 81 entities carry `sourceRef` + `contentHash`. The rest predate the mechanism and hold `conversationId` + timestamp only. […] it means a reference-only notification payload has no sources link to offer for two-thirds of the graph. […] `nw-schema-behind-practice` needs superseding — its title is the error and at least four of its specific claims are wrong."

**User:** please proceed
