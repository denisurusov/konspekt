<!-- konspekt source excerpt
     Verbatim transcript of the notifier event-set exchange, konspekt,
     2026-07-20. Both sides included. This header is the only editorial text;
     everything below the rule is verbatim as written by each party, with
     omissions within a turn marked […]. No included line is paraphrased. -->

---

**Claude:** "**The notifier event set.** You said yes to it changing; the shape is still mine to propose, so here it is rather than in the repo: Add a fourth event, `status_changed` — an entity whose `status` differs from its value in the parent commit. Cheap: the differ's front-matter reader already extracts scalars, so it's one more field and one more comparison. Two things I'd want your call on before writing it: **1. Payload.** The other three events carry only id/type/event/links. A bare 'status_changed: task-foo' is useless — you'd need `active → resolved` in the body. Status is an enum, not entity text, so I read this as compatible with reference-only rather than a breach. But it is a widening, and `nw-notification-payload-is-reference-only` should probably be amended rather than silently stretched. **2. Subscription granularity.** 'Tell me when anything is resolved' is the real want, not 'tell me when any status changes.' That needs a per-subscription filter — and `to:` already means recipients in `notify.yml`, so the obvious field name is taken. `status_to: [resolved]` is ugly but unambiguous."

**User:** take care of these two items first, then go back to code_tracer
