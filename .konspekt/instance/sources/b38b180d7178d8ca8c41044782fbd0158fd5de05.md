<!-- konspekt source excerpt
     Verbatim exchange on instance upgradeability and portable notifications,
     konspekt, 2026-07-19. Both sides included. This header is the only
     editorial text; everything below the rule is verbatim as written by each
     party, with omissions within a turn marked […]. Nothing is paraphrased. -->

---

**User:** ok, got the email. now to my other question from before - if I go to code_tracer (which had konspekt instance setup) - I won't get any emails, will I? I am saying the setup step should include the workflow setup

**Claude:** "Confirmed — you won't. `init.mjs` writes `.konspekt/instance/*`, `OPERATING.md`, `NOTES.md`, and the `AGENTS.md` stanza. No `notify.yml`, no script, no workflow. code_tracer has a graph and no watcher. […] I'd push back on it being unconditional, for one reason: **the workflow is GitHub-specific and konspekt isn't.** The whole design keeps the format transport-agnostic — that's what the manual re-upload probe protects. A scaffolder that always writes `.github/workflows/` quietly makes GitHub the assumed host, which is the same capture problem `nw-mcp-binding-needs-neutral-read` guards against on the read side. […] **The instance path is hardcoded.** […] **The connector can't install it.** […] **code_tracer's conformance is unverified.**"

**User:** this is an unresolved task, I think. actual a concept (konspekt-enabled repos must be upgradeable to the latest konspekt features) and then a task - make notifications portable
